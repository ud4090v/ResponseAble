/**
 * ResponseAble Background Service Worker
 * 
 * This service worker handles background tasks for the extension.
 * Currently minimal as most logic runs in the content script.
 */

// Background service worker is active
